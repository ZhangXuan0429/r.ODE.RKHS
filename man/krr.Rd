% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krr.R
\name{krr}
\alias{krr}
\title{Kernel ridge regression (KRR) with a user-supplied kernel}
\usage{
krr(X_train, y_train, X_test, kernel_func, sigma, lambda, gamma)
}
\arguments{
\item{X_train}{Numeric matrix (k x p). Training inputs.}

\item{y_train}{Numeric vector of length k. Training targets.}

\item{X_test}{Numeric matrix (m x p). Test inputs to predict on.}

\item{kernel_func}{Function taking (X1, X2, sigma) and returning a kernel matrix.}

\item{sigma}{Positive scalar bandwidth passed to \code{kernel_func}.}

\item{lambda}{Positive scalar base regularization.}

\item{gamma}{Positive scalar scaling for the ODE penalty; forms \eqn{\lambda_{\mathrm{eff}}}.}
}
\value{
A list with elements:
\describe{
\item{alpha}{Numeric vector of coefficients \eqn{\alpha} (length k).}
\item{y_pred}{Numeric vector of predictions on \code{X_test} (length m).}
}
}
\description{
Solve the linear system \eqn{(K + \lambda_{\mathrm{eff}} I)\alpha = y} and make predictions.
}
\examples{
X <- matrix(rnorm(20), nrow = 10); y <- rnorm(10)
fit <- krr(X, y, X, kernel_func = rbf_kernel, sigma = 1, lambda = 1e-2, gamma = 1)
head(fit$y_pred)

}
